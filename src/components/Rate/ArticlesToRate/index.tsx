'use client';

type ArticlesToRateProps = {
	handleOptionClick: (option: string) => void;
};


export default function ArticlesToRate({ handleOptionClick }: ArticlesToRateProps) {
	return (
		<div>
			<div className="container mb-6 mt-8 flex justify-center">
				
				</div>
        
		</div>
	);
}
